import React from "react";
import styles from "./footer.module.css";

const Footer = (props) => (
  <footer className={styles.footer}>
    <h2 className={styles.title}>Code Your Dream</h2>
  </footer>
);
export default Footer;
